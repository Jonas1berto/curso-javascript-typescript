//               -4       -3        -2       -1
//                0        1         2        3
const nomes = ['Jonas', 'Junior', 'Felip', 'Savio']

// splice(indice onde começa, delete, elem1, elem2, elem3)
// const removidos = nomes.splice(0, 2)

// adicionando 'João' no indice 2
// nomes.splice(2, 0, 'João')

// Substituindo o elemento a partir do indice 2, e adicionando novos.
//nomes.splice(2, 2, 'Humberto', 'Gabriel')

// Simulando o pop
// nomes.splice(-1, 1)

// Simulando o shift
// nomes.splice(0, 1)

// Simulando o push
// nomes.splice(nomes.length, 0, 'Maria')

// Simulando o unshift
// nomes.splice(0, 0, 'Valeska')
console.log(nomes)
