// Strings, numbers, undefined, null, boolean

const nome = 'Jonas' // String
const nome1 = "Jonas" // String
const nome2 = `Jonas` // String
const num1 = 10; // number
const num2 = 10.52 // number 
let nomeAluno; // undefined -> não aponta para nenhum local na memória
let sobreNomeAluno  = null; // Nulo -> não aponta para nenhum local na memória
const aprovado = false; // Isto é um valor Boolean = true, false (lógico)